# إعداد المشروع - أمواج Resort

## متطلبات التشغيل

- Node.js 18 أو أحدث
- pnpm (مدير الحزم)

## هيكل المجلدات المطلوب

قبل تشغيل المشروع، تأكد من نقل مجلد `assets` الموجود حاليًا إلى مجلد `public`:

```
public/
└── assets/
    ├── resort1/
    │   ├── 1.jpeg
    │   ├── 1.mp4
    │   ├── 2.jpeg
    │   ├── ... (جميع صور resort1)
    │   └── 21.jpeg
    └── resort2/
        ├── 1.jpeg
        ├── 2.jpeg
        ├── ... (جميع صور resort2)
        └── 15.jpeg
```

### خطوات نقل المجلدات

1. انتقل إلى مجلد المشروع
2. انقل مجلد `assets` الحالي إلى داخل مجلد `public`:
   ```bash
   # في Windows PowerShell
   Move-Item -Path "assets" -Destination "public\assets"
   ```

   أو يدويًا:
   - انسخ مجلد `assets` بالكامل
   - الصقه داخل مجلد `public`
   - احذف مجلد `assets` الأصلي (الموجود خارج `public`)

## خطوات التثبيت والتشغيل

### 1. تثبيت pnpm (إذا لم يكن مثبتًا)

```bash
npm install -g pnpm
```

### 2. تثبيت المكتبات

```bash
pnpm install
```

### 3. تشغيل خادم التطوير

```bash
pnpm dev
```

سيتم فتح الموقع على: [http://localhost:3000](http://localhost:3000)

### 4. بناء المشروع للإنتاج

```bash
pnpm build
```

### 5. تشغيل نسخة الإنتاج

```bash
pnpm start
```

## الصفحات المتاحة

- `/` - الصفحة الرئيسية مع عرض الشاليهات
- `/resort/resort1` - تفاصيل الشاليه VIP
- `/resort/resort2` - تفاصيل الشاليه العادي
- `/booking/resort1` - صفحة حجز الشاليه VIP
- `/booking/resort2` - صفحة حجز الشاليه العادي
- `/policy` - صفحة القوانين والشروط

## المميزات

✅ واجهة مستخدم حديثة وجذابة
✅ تصميم متجاوب (Responsive) لجميع الأحجام
✅ معرض صور تفاعلي (Image Gallery)
✅ عرض شريط صور متحرك (Hero Slider)
✅ نظام حجز متكامل
✅ حساب تلقائي للأسعار حسب نوع اليوم
✅ صفحة قوانين وشروط الحجز
✅ تحسين SEO
✅ دعم اللغة العربية بالكامل
✅ استخدام TypeScript للأمان والجودة

## التقنيات المستخدمة

- **Next.js 14** - إطار عمل React
- **TypeScript** - لغة برمجة آمنة
- **Tailwind CSS** - مكتبة تصميم
- **Embla Carousel** - مكتبة العرض الشرائحي
- **Lucide React** - أيقونات
- **date-fns** - معالجة التواريخ
- **Cairo Font** - خط عربي احترافي

## تخصيص الموقع

### تغيير معلومات الاتصال

عدّل ملف `src/components/layout/Footer.tsx` و `src/components/layout/Header.tsx`:

```typescript
// استبدل
<a href="tel:+973xxxxxxxx">+973 XXXX XXXX</a>
// بـ
<a href="tel:+97312345678">+973 1234 5678</a>
```

### تعديل بيانات الشاليهات

عدّل ملف `src/data/resorts.ts` لتغيير الأسعار، الوصف، أو المميزات.

### تغيير الألوان

عدّل ملف `tailwind.config.ts`:

```typescript
colors: {
  primary: {
    DEFAULT: '#0ea5e9', // اللون الأساسي
    dark: '#0284c7',
    light: '#38bdf8',
  },
}
```

## استكشاف الأخطاء

### المشكلة: الصور لا تظهر
**الحل**: تأكد من نقل مجلد `assets` إلى داخل `public` كما هو موضح أعلاه.

### المشكلة: أخطاء TypeScript
**الحل**: قم بتشغيل `pnpm install` أولاً لتثبيت جميع المكتبات.

### المشكلة: البناء يفشل
**الحل**: تأكد من وجود Node.js 18 أو أحدث: `node --version`

## الدعم

للمساعدة أو الاستفسارات، يرجى مراجعة الكود أو التواصل مع المطور.

---

**أمواج Resort** - موقع حجز شاليهات فاخرة © 2025
